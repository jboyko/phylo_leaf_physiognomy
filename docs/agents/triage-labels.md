# Triage vocabulary

No remote label inventory was needed for the September 2026 local repairs.
The skill's default role names are `needs-triage`, `needs-info`,
`ready-for-agent`, `ready-for-human`, and `wontfix`. Before a future remote
triage operation, inspect existing labels and map those roles to the actual
repository vocabulary; do not assume these labels already exist or create
duplicates automatically.
