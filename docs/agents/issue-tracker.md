# Issue tracker

The existing origin is `git@github.com:jboyko/phylo_leaf_physiognomy.git`.
Use `gh` for explicitly requested issue operations. Local fixes do not require
creating issues or posting comments. For multiline issue or PR bodies, use a
temporary file and `--body-file`. Do not send collaborator messages without
explicit authorization.
